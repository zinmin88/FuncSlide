% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/FuncSlide.R
\name{sliding_window}
\alias{sliding_window}
\title{Apply a sliding window function across a series}
\usage{
sliding_window(data, column, window_size, window_fun, output_col, ...)
}
\arguments{
\item{data}{Data frame containing the time series.}

\item{column}{Character. Name of the column to process.}

\item{window_size}{Integer. Number of observations in each window.}

\item{window_fun}{Function to apply to each window.}

\item{output_col}{Character. Name of the output column to create.}

\item{...}{Additional arguments passed to the window function.}
}
\value{
A data frame with an added output column containing the sliding
window results.
}
\description{
Iteratively applies a selected window function to consecutive windows
in a chosen column of a data frame and stores the results in a new column.
}
\note{
To compute multiple outputs (e.g., mean, binary, threshold),
this function must be called separately for each window function.
}
\examples{
\dontrun{
data <- sliding_window(
  data = data,
  column = "acceleration",
  window_size = 100,
  window_fun = window_mean,
  output_col = "mean"
)
}
}
